# NoProto PHP

Native PHP implementation placeholder.

This package should target the shared specification in `../../spec/` and pass the conformance suite in `../../conformance/`.

Suggested initial scope:

- Parse supported NoProto schemas.
- Encode and decode buffers compatible with `no_proto_rs` `0.9.60`.
- Export and import JSON-compatible values.
- Run the shared conformance fixtures in the package test suite.
